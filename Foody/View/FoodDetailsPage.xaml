<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="Foody.View.FoodDetailsPage"
    xmlns:local="clr-namespace:Foody.ViewModel">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
        <StackLayout.BindingContext>
            <local:FoodDetailsViewModel/>
        </StackLayout.BindingContext>
            <CollectionView x:Name="FoodDetailsView" ItemsSource= "{Binding FoodItems}">
            <CollectionView.ItemTemplate>   
                <DataTemplate>
                   <Grid Padding="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="50" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Image Grid.Row="0" Grid.ColumnSpan="3" Source="{Binding Image}" HeightRequest="125" Aspect="AspectFill"/>
                        <Label Grid.Row="1" Grid.Column="0" Text="{Binding Name}" FontAttributes="Bold"></Label>
                        <Label Grid.Row="2" Grid.Column="0" Text="{Binding Description}"></Label>
                    </Grid>                            
                 </DataTemplate>
            </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout> 
    </ContentPage.Content>
</ContentPage>
